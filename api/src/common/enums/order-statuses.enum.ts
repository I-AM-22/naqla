export enum ORDER_STATUSES {
  WAITING = 'waiting',
  READY = 'ready',
  ACCEPTED = 'accepted',
  REFUSED = 'refused',
  ON_THE_WAY = 'on the way',
  DELIVERED = 'delivered',
  CANCELED = 'canceled',
}
