// import { Injectable, PipeTransform } from '@nestjs/common';

// @Injectable()
// export class PhotoPipe implements PipeTransform<string, Promise<string>> {
//   async transform(photo: string): Promise<string> {
//     const dir = photo.substring(photo.indexOf('/photos'));
//     const path = `public${dir}`;
//     return path;
//   }
// }
